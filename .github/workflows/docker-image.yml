name: Docker Image CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - run: docker build . -t asheer/node-uat-testing:$(date +%s) --file Dockerfile.dev 
    - run: docker run -e CI=true  asheer/node-uat-testing:$(date +%s) npm test 

